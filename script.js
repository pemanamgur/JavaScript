//plaindrome
const arr = ["1", "2", "3"];
// console.log(arr.toString());
console.log(arr.reverse());
let newStr = arr.join("");
console.log(newStr);

//callback
function abc(a, b) {}

abc("1", () => {});

//higher oreder function accepts callback
//json client server   => there is no function
//object bitra function is method.
//function is independent and mehtod is built in .
//V-dom real dom change
//js is a loosely typed language  and single thread;
//array , object , maps , hashmap;
//patch and options also options -> prefied request   patch->update garnu banda agadi Put->update
//get bata update garnu milcha tara security .
//axios and fetch
